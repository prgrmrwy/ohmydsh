## 1. 移除入口处的二次确认

先做这一组：它与轮盘渲染无耦合，可独立验证与回滚。

- [x] 1.1 从 `src/host/skill-bundle.ts` 移除 `petConfirm` frontmatter 字段、解析逻辑，以及 `pet.confirm` 派生值。
- [x] 1.2 从 `src/client/overlay.tsx` 移除两段式确认流程（`pendingConfirm` 状态、`— confirm?` 提示、二次点击分支），改为单击直接调用。
- [x] 1.3 从 `skills/examples/*/SKILL.md` 移除 `petConfirm` 声明。
- [x] 1.4 添加测试：`SKILL.md` 中残留的 `petConfirm` 声明被忽略而非报错，Skill 仍可正常检查与注册。这保证既有 Skill 无需修改即可加载。
- [x] 1.5 添加测试：点击能力只需一次即发起 Invocation，不存在需要二次确认的分支。

## 2. 轮盘几何与渲染

- [x] 2.1 新增几何模块：由能力数量推导分圈（容量 6/8/10，上限 24，超出不渲染），并按桌宠尺寸计算各圈内外半径。
- [x] 2.2 实现扇环 SVG 路径生成。注意单个能力时扇区跨越 360°，标准圆弧路径会退化为一个点，需用双圆弧构造完整圆环。
- [x] 2.3 实现标签定位与旋转：切向对齐后，若归一化角度落在 90°–270°（会上下颠倒）则再翻转 180°。
- [x] 2.4 实现标签截断：按该扇区实际弧长计算可容纳字数，优先使用 `petLabel`，超限截断加省略号。
- [x] 2.5 添加测试：分圈逻辑覆盖 1、2、3、6、7、14、15、24、26 个能力，确认 26 个时兜底截到 24。
- [x] 2.6 添加测试：底部扇区（中角 90°）的标签旋转角为 0°，即完全正立；并覆盖若干角度确认无一倒立。
- [x] 2.7 用 `src/client/styles.ts` 承载扇环与标签样式，移除矩形列表的 `.dshpet-item` 相关规则。所有选择器保持 `dshpet-` 前缀。

## 3. 悬停与收起判定

- [x] 3.1 唤起绑定到桌宠本体元素，而非外层容器；容器设 `pointer-events: none`，仅桌宠与扇区可交互。
- [x] 3.2 收起改用几何判定：指针到桌宠中心的距离超过**已渲染最外圈**半径即收起，不设延迟。
- [x] 3.3 实现逐圈渐入：第一圈立即可见，其后每圈延迟 0.08s。
- [x] 3.4 添加测试：可保持半径随实际渲染圈数变化（6 个能力时为一圈半径，15 个时为三圈半径），而非恒定三圈。这是原型中复现过的缺陷。
- [x] 3.5 添加测试：指针位于桌宠之外、轮盘收起时，不唤起轮盘。
- [x] 3.6 添加测试：容器不拦截指针事件，底层页面在轮盘收起时仍可交互。

## 4. 任务面板简化

- [x] 4.1 将每个 Task 改为单一可点击行，点击打开 executor session。
- [x] 4.2 移除 `Open source`、`Archive`、`Cancel` 按钮及其 API 调用；移除 `SourceChip` 组件与样式。
- [x] 4.3 添加测试：面板行可点击并触发打开 executor session，且不存在归档或取消入口。
- [x] 4.4 验证归档同步链路仍然生效：在 executor session 归档终态 Task 后 Pet 同步归档；非终态 Task 保持活跃并给出诊断。该链路本变更前已存在，此处只做回归确认。

## 5. 设置页容量上限

- [x] 5.1 已启用 Skill 达到 24 个时阻止继续启用，并说明原因。
- [x] 5.2 添加测试：达到上限后启用第 25 个被拒绝，已启用 Skill 不受影响。
- [x] 5.3 添加测试：存量启用数超过 24 时轮盘只渲染前 24 个，其余 Skill 仍可管理，系统不报错。

## 6. 验证与部署

- [x] 6.1 运行包内 `npm run typecheck`、`npm run build`、`npx vitest run`。
- [x] 6.2 运行仓库 `npm test` 与 `npm run check:artifacts`。
- [x] 6.3 对新增测试做反向验证：逐条把实现改坏（固定三圈半径、绑定容器唤起、去掉旋转翻转、恢复二次确认），确认对应测试确实失败后还原。测试通过不等于测试有效。
- [x] 6.4 强制重装部署到 `$DSH_HOME` 并比对产物哈希，确认浏览器加载的是新构建。
- [x] 6.5 在隔离 DSH home 中启动 Host 验证轮盘实际渲染与交互；不要在用户的日常 Host 上做破坏性试验。
- [ ] 6.6 询问用户后重启其日常 Host，确认轮盘、面板与设置页在真实环境中的表现。
