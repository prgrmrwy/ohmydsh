> 说明:本 change 为已落地改动的补充规范。实现与验证在提案前已完成,下方勾选项均对应实际执行过的改动与检查。

## 1. 初始化脚本

- [x] 1.1 在 `scripts/bootstrap.sh` 引入纯 bash `version_lt`:按 `.` 分段十进制比较,剥离 `-` 预发布后缀,缺省段补 0,用 `10#` 规避前导零被解释为八进制
- [x] 1.2 用 `MIN_NODE=22.0.0` / `MIN_NPM=10.0.0` 替换精确相等判断,低于最低版本时报错退出并给出实际版本、最低要求与升级方式
- [x] 1.3 从 `.nvmrc` 读取推荐版本(去 `v` 与空白,文件缺失时回退内置默认),实际版本与推荐值不同时仅打印提示并继续
- [x] 1.4 更新脚本头部注释,写明「最低版本准则、`.nvmrc` 为推荐值」

## 2. 版本声明对齐

- [x] 2.1 根 `package.json` 的 `engines` 改为 `node >=22.0.0` / `npm >=10.0.0`
- [x] 2.2 移除 `package.json` 的 `packageManager: "npm@11.6.2"`,避免 corepack 在 `engines` 之外硬锁精确 npm
- [x] 2.3 同步 `package-lock.json` root 条目的 `engines`(仅该两行,不引入无关的依赖标记重排)
- [x] 2.4 确认 `.npmrc` 的 `engine-strict=true` 与 `.nvmrc` 内容无需改动,且新 `engines` 范围与脚本阈值一致

## 3. 文档

- [x] 3.1 README「从零开始」前置要求改为 **Node >= 22 + npm >= 10(推荐 `.nvmrc` 版本)**,并说明高于最低版本仅提示不阻塞

## 4. 验证

- [x] 4.1 `version_lt` 边界用例:`20.0.0<22.0.0`、`22.0.0>=22.0.0`、`24.16.0>=22.0.0`、`22.1>=22.0.0`、`9.9.9<10.0.0`、`10.0.0-beta.1>=10.0.0`、`24.02.1>=22.0.0`、`21.99.99<22.0.0` —— 全部符合预期
- [x] 4.2 真实环境(node v24.16.0 / npm 11.13.0)运行 `./scripts/bootstrap.sh`:通过并打印推荐版本提示,`npm ci` 完成,退出码 0
- [x] 4.3 以伪造的 node v20.11.0 运行 bootstrap:按预期报「版本过低」并以退出码 1 终止,未执行依赖安装
- [x] 4.4 `npm test`(20/20 通过)与 `npm run check:artifacts`(通过)
