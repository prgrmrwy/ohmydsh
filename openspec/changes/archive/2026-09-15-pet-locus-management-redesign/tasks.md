## 1. 纯函数层：`src/client/locus-view.ts`

- [x] 1.1 新增 `packages/dsh-pet/src/client/locus-view.ts`，导出短码派生：`locusShortCode` / `sessionShortCode` / `endpointShortCode` / `workspaceShortCode`，按 design D2 的规则表实现（`oc_`/`omt_` 前缀后前 4 位；`session-` 后第一段前 4 位；`locus-…` 取**末段**前 4 位；workspace uuid 第一段前 4 位），并处理同页冲突时的显示层延长（4 → 6 → 8 位）。
- [x] 1.2 实现入口家族聚合 `groupLociByEndpoint(snapshot)`：以 `discovery.byEndpoint[].current` 定当前代、`history` 定历史代，**不自行按 generation 推断当前代**（design D1）。
- [x] 1.3 实现两个读法的分组 `groupByWork(snapshot)`（父会话 → 入口，含 `parentLocusId` 话题嵌套）与 `groupByEntry(snapshot)`（入口 → 父会话 + 会话），两者共用同一批行数据（design D3）。
- [x] 1.4 实现可用性/状态文案映射与兜底名规则 `entryDisplayName(endpoint, groupName)`：有名称 → 名称 + 短码；无名称 → 有 `threadId` 为 `话题 · xxxx`，否则 `入口 · xxxx`；**MUST NOT** 在 `chatType` 未知时输出「群」（design D8、D9）。
- [x] 1.5 实现父会话状态筛选 `filterEntries(entries, filter)`：默认只保留 `availability === 'available'` 的父会话，并返回被隐藏的父会话数量与其下入口数量（供列表内出口使用）；筛选为纯函数，不修改输入（design D5）。
- [x] 1.6 为上述纯函数编写单测（新增 `test/locus-view.test.ts`）：覆盖多代聚合成一行、话题按 `parentLocusId` 嵌套、同名两个入口仍可区分、`chatType` 未知不输出「群」、跨父会话代次、归档父会话筛选与隐藏计数。
- [x] 1.7 实现状态词汇映射并把**全部六个**生命周期状态（`provisioning` / `active` / `switching` / `stopped` / `invalid` / `retired`）映射为文字 + 节点色；`provisioning` 必须映射为「准备中」而非空白或「活跃」（主干按需建树后它是真实瞬时态，且可能因 provisioning 失败长期停留，见主干 BACKLOG B040）。单测覆盖六态全覆盖与未知状态不静默。
- [x] 1.8 实现筛选分组映射：父会话状态 = 可用 / 已归档 / 未核验（默认只勾「可用」）；入口状态 = 活跃 / 准备中·切换中 / 已停止·已失效 / 已退役（默认全勾，等于不过滤），并附各分组计数。单测覆盖分组归属与计数。

## 2. LocusTab 重写：读法与行渲染

- [x] 2.1 重写 `LocusTab`（`src/client/settings.tsx`）：页首改为总览 + 读法切换（`按工作` / `按入口`）+ 搜索框；读法状态为组件本地状态。
- [x] 2.2 实现父会话分组头：会话标题（链接样式，点击打开父会话）、短码复制、可用性读数（可用 / 已归档）、工作区与路径；父会话只渲染一次。
- [x] 2.3 实现入口行：关系轨（1px 竖轨 + 节点，节点即状态）+ 两行式布局（身份/徽标 + `状态 · 只读` / `会话 <名称> <短码>`）+ 右侧固定动作列（`会话 ↗` / `飞书 ↗`，按入口读法另有 `父会话 ↗`）。
- [x] 2.4 话题入口行按 `parentLocusId` 嵌套渲染并标注继承来源；话题行的飞书跳转使用话题链接，无法证明话题身份时退化为所属群并说明（design D3、Risks）。
- [x] 2.5 归档 / 不可达目标的跳转必须不可用并就地说明原因，MUST NOT 产生一次静默跳到首页的导航（spec「双向可达且目标不可达时 fail closed」）。
- [x] 2.6 历史代折叠：主行显示 `历史 N 代`，展开后逐代显示代数、状态、时间与其子会话，并提供打开对应会话的入口。
- [x] 2.7 行内不得出现快照 generation；代际文案说明新代的来源（重建 / 切换来源）与「新一代默认回到只读、不继承旧代写权限」。

## 3. 筛选与"不静默隐藏"

- [x] 3.1 页首加入筛选控件：漏斗图标 + **当前条件文本**（父会话维度 + 入口状态维度，如「父会话：可用 · 入口：在服务」）+ `aria-pressed`，不使用无语义图标（spec「按父会话与入口状态筛选」）。
- [x] 3.2 实现筛选浮层：`父会话状态`（可用 / 已归档 / 未核验，带计数，为 0 的项保留但置灰）与 `入口状态`（活跃 / 准备中·切换中 / 已停止·已失效 / 已退役，计数来自快照，不估算）。
- [x] 3.3 浮层交互：只提供一种关闭方式 —— 点击面板外部（面板自身不带关闭按钮，也不绑 Esc）；筛选只影响呈现，不写入任何持久化状态，也不得被表述为已修改数据。
- [x] 3.4 披露只由筛选控件承担：按钮收起态写明「父会话维度 · 入口状态维度」，浮层按桶给出全量快照计数与一键重置；列表内**不**再加"隐藏出口"行（design D5：该行与控件重复，且把入口整体下推）。
- [x] 3.5 默认筛选收敛为「父会话可用 + 入口仍在服务」：终止态（`stopped` / `invalid` / `retired`）默认不呈现。补偿三件套必须同时存在：筛选控件的收起态表达入口状态维度（`入口：在服务`）、浮层里终止态桶带计数、显示后「重建」位于行本身（design D5）；无可见行时空态文案点名该勾哪个桶；头部计数改为快照全量，避免「关联 0 个入口」这种读起来像"没了"的表述。
- [x] 3.6 头部布局：标题行 = `关联` + 右端筛选控件；计数描述单独一行；工具行 = 「按工作/按入口」+ 搜索框。标题不收缩不换行（此前被压成一列一个字），控件与标题同行、放不下时整块换行。

## 4. 折叠区与默认 Q&A 只读化

- [x] 4.1 每行实现「标识符」折叠区：locus / chat / thread / 父会话 / 会话 / workspace 的完整标识，全部可一键复制；短码在任何路径下都不得作为请求入参（design D2）。
- [x] 4.2 每行实现「操作」折叠区：权限分段控件（只读 / 可写，可写在执行根未确认时禁用并给出原因）、确认执行根、**停止关联**（三方收敛后的唯一退出动作，危险项用文字色表达，不用红边框）；Host 拒绝时不做乐观改写。「重建」只在终止态（失效/停止/退役）合法，因此不做常驻禁用控件，而是渲染在终止态行的行尾 —— 这类行默认被筛选隐藏，恢复路径不能埋在两层折叠之后。
- [x] 4.3 折叠区保留来源与时间、权限与核验时间、执行根与锚点状态，以及 owner 投影 / 询问台账的如实呈现（未提供时明说未提供，不伪造询问记录）。
- [x] 4.4 默认 Q&A 改为行内徽标（蓝环节点 + 文字徽标双通道），删除独立的「默认 Q&A」分组与其重复卡片；父会话尚无默认 Q&A 时在同一分组内说明创建路径位于 Pet 轮盘（spec「默认 Q&A 在管理面只读呈现」）。
- [x] 4.5 将「发现关联」三选择器表单降级为折叠的手工查询入口（粘贴 id 而非手输），并把结果卡改为自证式：入口（角色 + 名称 + 群级/话题/继承关系）→ 代际与状态 → 父会话/会话 → 跳转；空结果如实说明，不退回裸 ID 列表。（2026-09-15：所有者判定暂时无需求，整块以 `DISCOVERY_FOLD_ENABLED = false` 停用；代码与 Host 路由保留，见 design D3。）
- [x] 4.6 补充一行说明「为什么这里没有删除」：解绑/归档/停止只停止服务并保留主/子会话历史与飞书资源，历史只被聚合与折叠、不被清掉（对应 BACKLOG B030 要点 1）。

## 5. 移除设置页的新建外部资源入口

- [x] 5.1 移除绑定 endpoint 表单（Chat ID / Thread ID / 主会话 ID / Workspace ID / 父级 locus ID 与「绑定（默认只读）」按钮）。
- [x] 5.2 移除默认 Q&A 的两组创建按钮（无 Q&A 时按裸 session id 并排渲染的那组，以及条目卡片上的「创建/打开默认 Q&A（此主会话）」）。
- [x] 5.3 确认移除后不再有需要手输 endpoint/session 标识的可执行创建入口，且生命周期动作（解绑/归档/停止/重建/权限/确认执行根）全部保留。

## 6. 样式

- [x] 6.1 在 `src/client/styles.ts` 新增关系轨、入口行、短码 chip、徽标、导航按钮、筛选浮层、历史代折叠区的样式；全部使用 DSH 主题变量与字面回退值，**不新增自定义颜色**。
- [x] 6.2 明确收缩规则：身份列不参与收缩（`flex: 0 0 auto` + 最小宽度），元信息列先截断；长标题截断并保留 `title` 全文。头部标题同样不收缩不换行，计数与宽控件（筛选）在放不下时换行，而不是把标题压成竖排。
- [x] 6.3 复核 568px 内容列宽下的表现，并覆盖更窄宽度（移动端）不溢出；`.callout` 等含行内 `<b>`/`<code>` 的块 MUST NOT 用 `display:flex`（会把行内元素拆成并排的列）。
- [x] 6.4 无障碍底线：折叠使用原生 `<details>`；状态永远"文字 + 颜色"双通道；焦点可见；`prefers-reduced-motion` 下无动画。

## 7. 测试与校验

- [x] 7.1 更新 `test/client.test.ts` 中针对 `settings.tsx` 的源码文本断言（大批会因重写失效），保留其要证明的不变量（不读取凭据、不传浏览器所有者身份、不回退旧模型）。
- [x] 7.2 新增断言：首行不以 `oc_` / `omt_` / `session-` / `locus-` 开头；同一 locus 在页面中只渲染一次；页签信息架构仍为六个稳定页签。
- [x] 7.3 运行 `packages/dsh-pet` 的 `npm run typecheck`、`npm test`、`npm run build`，并运行仓库根 `npm test` 与 `npm run check:artifacts`。
- [x] 7.4 用真实快照做一次目视验收：面板默认视图不再滚屏即可看完当前入口；被默认筛选排除的入口/父会话通过筛选控件（收起态条件文本 + 浮层计数）发现，且列表里没有多余的出口行。**2026-09-15 所有者目视验收通过**（期间按反馈收敛了头部三段布局、去掉列表内出口行、停用反查折叠区）。
- [x] 7.5 验收与按需建树（主干 `pet-locus-on-demand-tree`）的交互：拉 bot 入群但不 @ 时管理面**不出现**该入口（正确结果，不是缺失）；首个 @ 建树期间该入口以「准备中」出现；`/bind` 一次建对的入口 `来源` 显示为显式绑定。**2026-09-15 所有者实测三项均确认**。

## 8. 规范与 backlog 回填

- [x] 8.1 1 复核 `openspec/specs/pet-locus-collaboration/spec.md` 与 `openspec/specs/dsh-pet/spec.md` 在归档后反映最终行为；确认本 change 未改动 `src/host/**` 与 `src/wire.ts`（`git diff --stat` 证明）。
- [x] 8.2 回填 BACKLOG：B026 / B030 / B033 状态推进（实施后转「已完成」并注明落地位置）；本 change 未覆盖项（群名 / 话题名 / `chatType` 的获取）保持原条目与决定记录不变。
- [x] 8.3 核对 backlog 编号：本 change 登记的「父会话归档回执」条目已编号为 **B042**（主干 `pet-locus-on-demand-tree` 已占用 B040 用于 provisioning 补偿阻塞、B041 用于侧栏 workspace 时序），合入时不得再产生重号。

## 9. 回归修复

- [x] 9.1 修复「重建」被 Host 拒绝 `Unknown request field 'locusId'`：本 change 重写面板时把共享的 mutation fence（含 `locusId`）用 spread 一并塞进了 rebuild，而 rebuild 的 action schema 用 `endpoint` + `expectedLocusId` 定位代际、没有 `locusId` 字段。TypeScript 不会对 spread 属性做过量属性检查，所以编译通过、运行时被 `strictBody` 拒绝。这是本次重写引入的回归（`d6f8a8b` 的旧实现只发三个 `expected*` 字段）。
- [x] 9.2 补上两侧契约的守卫：`LOCUS_ACTION_FIELDS` 由 `src/host/routes.ts` 导出，`test/route-groups.test.ts` 走真实点击、把面板能发出的每个 action 载荷与其允许字段逐一比对（rebuild 走终止态行，scope / confirm-anchor / stop 走「更多」）。反向验证：临时把 spread 放回去，两条用例如期失败。
