## 1. 长驻浏览服务的进程管理

- [ ] 1.1 新增独立于 `runKernel()` 的长驻进程 runner（不修改现有 runner 的 timeout/SIGTERM 语义）
- [ ] 1.2 启动参数：强制本地界面模式、抑制自动开浏览器、注入 `MEMEX_HOME` 指向目标库、沿用 `LC_ALL=C/LANG=C`
- [ ] 1.3 stdin 使用 `'pipe'` 且不写不关（用 `'ignore'` 会让长驻进程立即 EOF 退出，见 pitfalls 第 2 条）
- [ ] 1.4 就绪判定：解析 stdout 的监听行取得**实际**地址；解析不到即判为启动失败，不回退到"假定等于请求端口"
- [ ] 1.5 回收：SIGTERM 终止；注册在插件 dispose 上确保插件停止时全部终止；不按端口/命令行相似性猜测归属
- [ ] 1.6 单测：配置了远端的库不会走托管重定向分支（否则服务不存在）；端口漂移时取到实际端口；stdin 形态正确；dispose 后无残留

## 2. 服务注册表与按库隔离

- [ ] 2.1 维护「库 → 运行中的服务实例」映射，重复请求同一库复用既有实例而非启动第二个
- [ ] 2.2 确认一个实例只能访问其所属库（由 `MEMEX_HOME` 决定），补测试断言跨库不可达
- [ ] 2.3 实例异常退出时从注册表移除，下次请求重新启动
- [ ] 2.4 单测：并发请求同一库只启动一个实例；不同库互相隔离

## 3. 浏览地址注册点（dsh-memex 侧，不提任何注册方）

- [ ] 3.1 定义 dsh-memex **自己命名**的浏览地址注册点与契约（命名权归 dsh-memex），写进本仓 spec 作为唯一真相源
- [ ] 3.2 实现注册/注销与默认实现：未注册时给出本机地址；注册点 MUST NOT 进入 `inject`（loader 对不可解析依赖是静默不加载）
- [ ] 3.3 注册方晚于 dsh-memex 加载也生效；注册方卸载后恢复默认且不持有失效引用
- [ ] 3.4 注册方抛错或未给出地址时**不回落本机地址**（跨机器下会解析到错误的机器），改为呈现失败原因
- [ ] 3.5 代码与配置中不出现任何注册方的 package 名 / 服务名 / 产品名（含约定服务名）
- [ ] 3.6 单测：无注册方给本机地址；有注册方用其地址；晚注册生效；卸载恢复默认；注册方抛错不给本机地址也不报成功

## 3b. 专用 shim package（跨机器路径；依赖 dsh-cockpit 侧能力先落地）

- [ ] 3b.1 新建 `packages/cockpit-memex-browse-shim/`，形态与依赖声明对齐既有 `packages/cockpit-worktree-open-shim/`（host 半区惰性，集成在 client 半区）
- [ ] 3b.2 探测两端并接线：读驾驶舱端口发布能力 + 注册进 dsh-memex 的注册点；任一端缺席则不产生效果且不抛未捕获错误
- [ ] 3b.3 服务读取一律用**完整 dotted name 的单次 `ctx.get('a.b')`**，禁止 `ctx.get('a').b`（Cordis 重路由点属性并强制 inject，会变成未捕获拒绝）
- [ ] 3b.4 顶层 `inject` 必须为空数组；监听服务变更以支持任意加载顺序，并在任一端消失时解除接线
- [ ] 3b.5 保持最薄：不校验、不拼装地址、不重试、不持状态、不缓存；失败原样上抛由 dsh-memex 呈现
- [ ] 3b.6 在 `dsh.yaml` 登记该 package，并写 README 说明存在理由与移除路径（对齐 `subscriptions-sandbox-shim` 惯例）
- [ ] 3b.7 单测：仅装一端时无效果且两端正常；两端就绪后接线；晚加载可接线；一端卸载后解除接线

## 4. 同源 launcher 路由

- [ ] 4.1 在 DSH 侧注册 launcher 路由（我们自己的资源；上游 HTML 仍在其自身端口根路径原样运行）
- [ ] 4.2 launcher 流程：校验入口 → 确保服务就绪 → 取得地址 → 跳转
- [ ] 4.3 `memory: false` 在**启动任何进程之前**拒绝，并说明重新开启的位置（对齐归档 design D20 的「拒绝先于 ensure」）
- [ ] 4.4 失败呈现：服务启动失败、跨机器且能力不可用两类情形就地说明原因；MUST NOT 给出可能指向其它服务的地址
- [ ] 4.5 单测：关闭记忆的库被拒且未 spawn；启动失败不跳转；跨机器不可用时不返回本机地址

## 5. 设置页「打开」动作

- [ ] 5.1 在入口**展开态**增加「打开」动作，与既有路径/远端动作并列；列表态保持不变
- [ ] 5.2 可见性规则：关闭记忆、内核不可用、库未物化三种情形不呈现（或禁用并说明），不呈现点击必然失败的按钮
- [ ] 5.3 点击行为：同步 `window.open` 到 launcher 路径，页面不进入等待态（避免弹窗拦截）
- [ ] 5.4 新增文案进 `locales.ts`，覆盖动作名与各降级说明
- [ ] 5.5 单测/组件测：三种不可用情形的渲染；点击不阻塞页面

## 6. 契约、文档与验收

- [ ] 6.1 更新 `dsh.yaml` 中 dsh-memex 的 note：新增运行依赖（按需拉起内核 serve）、stdout 解析的版本复核点、CDN 外链的已知限制
- [ ] 6.2 在 `docs/notes/` 记录内核 serve 的四条行为约束与本方案的应对（或在既有 pitfalls 文档中追加一节）
- [ ] 6.3 运行仓库检查：`npm test`、`npm run check:artifacts`、`node scripts/sync.mjs`（含幂等验证：连续第二次运行无变化），以及 package 内的 build/typecheck/test
- [ ] 6.4 真机验收（本机直连）：对一个已物化且开启记忆的库点击「打开」，新标签页呈现内核界面并可浏览卡片
- [ ] 6.5 真机验收（降级）：关闭记忆的入口无动作且直接请求被拒；未物化的库无动作；内核不可用时不误导
- [ ] 6.6 真机验收（跨机器）：待 dsh-cockpit `device-port-forward-seam` 落地并装上 shim 后，验证经注册方取得的地址可从宿主机浏览器访问；未装 shim 时验证呈现为「不可用 + 原因」，且移除 shim 后两端各自仍正常
- [ ] 6.7 确认 current specs 已同步最终行为后归档 change
