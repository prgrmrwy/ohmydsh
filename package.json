{
  "name": "ohmydsh",
  "private": true,
  "type": "module",
  "description": "DSH 定制仓:总配置 + 可插拔定制",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:artifacts": "node scripts/check-tracked-artifacts.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "semver": "^7.8.5"
  }
}
