# ohmydsh 默认镜像:直连 npmjs(registry.npmjs.org),不依赖任何内网源。
#
# 背景:autoUpdate 的版本检测(scripts/check-update.mjs)直连 npmjs 判断新版本,
# 安装 @deepseek-ai/dsh 时若被用户级 ~/.npmrc 指向的其他镜像覆盖,会与检测
# 源不一致——2026-08-21 升级 0.1.1-rc.1 时即因镜像子包未同步而失败
# (ETARGET / npx 锁竞争 ECOMPROMISED),导致 dsh 启动失败。
#
# 统一到 npmjs 保证「版本检测」与「安装」走同一来源,且全仓库(含 lockfile)
# 不依赖内网;需要特殊源的包可在此追加 scope 级覆盖。
registry=https://registry.npmjs.org/