# ohmydsh 默认镜像:直连 npmjs(registry.npmjs.org)。
#
# 背景:autoUpdate 的版本检测(scripts/check-update.mjs)直连 npmjs 判断新版本,
# 而安装 @deepseek-ai/dsh 走 npx/npm,若命中用户级 ~/.npmrc 指向的滞后镜像
# (如 bnpm.byted.org),会出现"检测到新版本但装不上"——2026-08-21 升级
# 0.1.1-rc.1 时该镜像 4 个子包(dsh-session-checkpoint-policy /
# dsh-session-query-sqlite / dsh-session-telemetry-otel /
# dsh-session-persistence-jsonl)未同步,导致 ETARGET、npx 缓存锁竞争
# (ECOMPROMISED),dsh 启动失败。
#
# 统一到 npmjs 保证「版本检测」与「安装」走同一来源;需要内网/镜像的包
# 可在此追加 scope 级覆盖,如 @some-scope:registry=http://mirror.example/。
registry=https://registry.npmjs.org/