# Changelog

## 0.1.0 - Unreleased

- Add an inert Host/Web bundle skeleton.
- Establish `core`, `contract`, `host`, and `client` dependency boundaries.
- Keep deployment disabled while the OpenSpec M1–M3 implementation and verification proceed.
