{
  "name": "@deepseek-ai/dsh-subagent",
  "version": "0.1.5-rc.2-locus-settlement-notice.1",
  "private": true,
  "type": "module",
  "description": "Fixed-source compatibility artifact re-derived against dsh-v0.1.5-rc.2: adds silent settlement, idle continuable creation, independent-v1 composition, and continuation-owned exact child Session access. Run `node build.mjs` to materialize it from the pinned upstream source.",
  "scripts": {
    "build": "node build.mjs"
  }
}
