{
  "name": "@deepseek-ai/dsh-subagent",
  "version": "0.1.2-rc.1-locus-settlement-notice.0",
  "private": true,
  "type": "module",
  "description": "Fixed-source compatibility artifact: adds silent settlement, idle child creation, and continuation-owned exact child Session access for unified locus. Run `node build.mjs` to materialize it from the pinned upstream source.",
  "scripts": {
    "build": "node build.mjs"
  }
}
