# Bundle self-activation patch: when this package is installed into a profile
# via `dsh plugin --profile web add file:<repo>/packages/subscriptions-sandbox-shim`
# (the mydsh sync path), this patch mounts the plugin's node half.
# Host-only plugin: no `dsh.client` declaration, so there is no client bundle.
- insert:
    - id: subscriptions-sandbox-shim
      name: dsh-subscriptions-sandbox-shim
