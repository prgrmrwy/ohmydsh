# dsh-sidebar-session-provider-icon Host entry: registers the `provider`
# session-projection unit as a cold-history fallback. The Web client bundle
# prefers the composer selector's live per-session provider/model state.
- insert:
    - id: sidebar-session-provider-icon
      name: dsh-sidebar-session-provider-icon
