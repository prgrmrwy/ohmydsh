# dsh-sidebar-session-provider-icon Host entry: registers the `provider`
# session-projection unit so per-session last-request provider/model flows to
# the Web client. The package also declares a Web client bundle (badge render).
- insert:
    - id: sidebar-session-provider-icon
      name: dsh-sidebar-session-provider-icon
