# zydsh 本地环境变量模板 — 复制为 .env.local 后按需修改(gitignored,bash 语法)

# 打开已安装的 DeepSeek Harness PWA,而非默认浏览器
# DSH_OPEN_APP="$HOME/Applications/Chrome Apps.localized/DeepSeek Harness.app"

# 局域网访问开关(可选;优先级高于 dsh.yaml 的 web.lan,不设则用 dsh.yaml)
# DSH_LAN=1     # 开启:绑定 0.0.0.0,启动打印局域网地址,同网络设备可访问
#               # ⚠ 局域网可驱动完整 agent 能力(bash/文件读写),仅可信网络开启
# DSH_LAN=0     # 关闭:仅 127.0.0.1

# SSH 隧道专用的远端/无头 DSH:让官方 directory-picker-auto 固定选择
# 浏览器内的远端目录浏览器,避免在无人值守的 VM 上弹原生系统选择窗口。
# 该值只需非空,且仅在 DSH 启动时采样;修改后需重启 DSH。
# SSH_CONNECTION=1

# 定制项按需开关(可选;dsh.yaml 中某定制声明了 enabledEnv 时才生效,
# 优先级高于该定制自身的 enabled,不设则用 dsh.yaml;改动后需 `dsh build`)。
# dsh-traex-bridge 是内部专属包(bnpm/内网鉴权),仓库默认 enabled: false,
# 只在有权限的机器上按需打开:
# DSH_TRAEX_BRIDGE=1   # 开启:登录/模型目录/推理走 ByteDance 内网服务
# DSH_TRAEX_BRIDGE=0   # 关闭(或不设):与仓库默认一致

# 其他示例:
# DSH_HOME="$HOME/.dsh"      # 自定义 DSH 数据目录
# DSH_PROFILE="web"          # 自定义 profile 名
